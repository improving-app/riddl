user Member is "The person that is being enrolled"
user App is "The applicaiton the user is using to perform this action"
user GatewayAPI is "The interface that accepts the request for processing by the
 backend"
