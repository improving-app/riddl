epic CreateTenant {
  user Member wants to "create a new Tenant" so that "I can use its features within my organization"
  ???
} with {
  described as "I am now the Owner of that Tenant."
}

//Tenant Gateway Stories
